# MEalWay-Restaurant
Project Technologies HTML: Structures the website content (forms, menus, headings).  CSS: Styles the presentation (colors, fonts, layout, design patterns).  Bootstrap: A CSS framework for responsive, mobile-first design.  PHP: The backend language; processes forms and application logic. SQL: Manages the database (stores menu items, bookings, etc.).
